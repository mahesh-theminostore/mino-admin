export interface VendorListModel {
  id: string;
  name: string;
  active: boolean;
  dob: string;
  phoneNumber: string;
  pinSet: boolean;
  verified: boolean;
  createdAt: string;
  updatedAt: string;
}
